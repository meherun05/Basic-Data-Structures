 #include <bits/stdc++.h>

using namespace std;

int main(){

    string s;
    cin >> s;
    // cout << s << " " << s.size(); // the size of the string;
    // cout << s << " " << s.max_size(); // return the maxium size the string can hold
    // cout << s << " " << s.capacity(); // return the current capacity the string have
    //  s.clear(); // clear the string
    // cout << s << " " ;
    
    return 0;
}