 #include <bits/stdc++.h>

using namespace std;

int main(){
    int n,q,l,r;
    cin >> n >> q;
    vector<int> v(n);
    for (int i = 1; i <= n; i++)
    {
        cin >> v[i];
    }
    for (int i = 0; i < q; i++)
    {
        cin >> l >> r;
    }
    
    
    
    
    return 0;
}